import { DisableAuthDirective } from './disable-auth.directive';

describe('DisableAuthDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableAuthDirective();
    expect(directive).toBeTruthy();
  });
});
